import React from "react";
import styles from "./style.scss";

const Header = () => (
    <header className={styles.Header}>
        <nav>
            <ul>

            </ul>
        </nav>
        <h1>Header</h1>
    </header>
)

export { Header };
